version: 2.1

orbs:
  win: circleci/windows@1.0.0

jobs:
  build:
    executor: win/vs2019
  branches:
    only:
      - take_home_test_ewantha
    steps:
      - checkout
      - working_directory: ~/MobileAppAutomationTest
      - run: ECHO "Hello"